import { defineStore } from "pinia";
// import { get<%= data.name %>Data } from "@/service";

const use<%= data.name %>Store = defineStore("<%= data.lowerName %>", {
  state: () => ({}),
  actions: {},
});

export default use<%= data.name %>Store;